import Layout from "@/components/Layout";
import { NotFoundPage } from "@/components/styles";

const NotFound = () => {
  return <NotFoundPage>404, page not found :(</NotFoundPage>;
};

export default Layout(NotFound);
